"use client";

// Static import of driver.js CSS — must be in a client component.
// Add <TourStyles /> to the root layout once to make it available everywhere.
import "driver.js/dist/driver.css";

export function TourStyles() {
  return null;
}
